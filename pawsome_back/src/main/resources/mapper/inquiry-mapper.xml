<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.inquiry.model.dao.InquiryDao">
	<select id="totalCount">
		select count(*) from inquiry
	</select>
	<select id="selectInquiryList">
		select * from (select rownum as rnum, b.* from(SELECT inquiry_no,inquiry_title,to_char(inquiry_reg_date)as inquiry_req_date,inquiry_type, member_email from inquiry order by inquiry_no desc)b) where rnum BETWEEN #{start} and #{end}
	</select>
</mapper>
